[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cp wireframes/flows.docker.ts wireframes/flows.ts"]

[start]
cmd = "node_modules/.bin/tsx bin/cli.ts dev --port ${PORT:-4800}"
